# Pin NumPy 1.x for stable wheels with Anaconda / older optional deps (numexpr, bottleneck, matplotlib).
# Use a project venv: python3.10 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
